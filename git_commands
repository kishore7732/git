1. git clone <url of the remote repo>
			>>> to clone the repo from a remote server lo local serber

2. git config --global user.name <kishore7732>
			>>> to add user name

3. git config --global user.email <kishore00110101004@gmail.com>
			>>> to add user email id

4. git config -l  >>> to check the username and email

5. git add <file name> 
		>>> will add the file to staging area

6. git add . 
		>>> will add the the files in present directory to staging area

7. git checkout -b <branch name>
			this will create a new branch and switches into it

8. git checkout <branch name>
			this will switch into the branch name given
 
9. git remote add origin https://<token generated>@<url of the repo>
		>>> to authenticate the remote repo to local machine to push without username and password

10. git push origin master
		>>> to push the changes to remote repo after commiting locally

